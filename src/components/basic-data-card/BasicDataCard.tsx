import React from 'react'

interface BasicDataCardProps {
    CardSchema: ICardSchema;
}

const BasicDataCard: React.FC<BasicDataCardProps> = ({ CardSchema }) => {
  return (
    <div>BasicDataCard</div>
  )
}

export default BasicDataCard